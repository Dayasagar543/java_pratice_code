public enum Status {// these are named constants instead of numbers and strings we do this
    // enums class cannot be extended but we can define our own variables ,methods
    // consturctors etc etc..
    Running, Failed, Pending, Success, overloaded;

}
