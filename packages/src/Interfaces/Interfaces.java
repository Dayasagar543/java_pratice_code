package src.Interfaces;

public class Interfaces {
    // Interfaces are of 3 types they are normal or general
    // interfaces,Functional(sam),marker Interfaces

    // INTERFACE
    // nomal interface is that there will 2 or more function declarations
    // example for function delcaration------------> public int run();

    // FUNCTIONAL INTERFACE
    // single abstract methodinterface which will have only one method inside of the
    // interface and can use an anotation that in functional interface
    // (SAM)

    // MARKER INTERFACE
    // an interface with no methods delcared in side it or a blank interface
}
