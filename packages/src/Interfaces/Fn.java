package src.Interfaces;

@FunctionalInterface
interface Fn {
    void run();
}

// if there is only one method delcalred in a file then it is called the
// functional interface and we use the above annotations @funcitonalInterface