package src;

public interface Computer {

    // Any variable declared in the interface prefixed with final or static .
    void code();// its not necessary to mention public abstarct cause its already or defaultly
                // added in an interface
}
