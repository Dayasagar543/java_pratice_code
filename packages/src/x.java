package src;

public interface x {

    // defaultly these variables are static and final so you can call them by using
    // the interface name without creating the object
    int age = 21;
    String name = "rahul";

    void greet();
} 